apiVersion: v1
kind: Service
metadata:
  name: pmo-copilot
  namespace: pmo-copilot
  labels:
    app: pmo-copilot
spec:
  type: NodePort
  selector:
    app: pmo-copilot
  ports:
    - name: streamlit
      protocol: TCP
      port: 8501
      targetPort: 8501
      nodePort: 30501
